package ch06.lecture.p01field;

public class MyClass1 {
	// 클래스에는 객체를 생성하기 위한 필드와 메소드가 정의되어 있다
	// 클래스는 stack 클래스가 생성하는 객체는 heap영역에 저장
	
	// 클래스는 주로 하나의 파일에 작성됨
	// 파일명은 클래스명과 같다
	
	// 값(field)에 대한 내용.... - 1) 정보저장
		//객체의 데이터가 저장되는 곳 
			// => 공간 형성(값을 입력하지 않으면 기본 초기값 설정)
		//(instance)field
	
	
	// 생성자(constructor)에 대한 내용.... - 2) 호출
		// new 객체 생성 시 초기화 역할 담당
		// static(고정값) field
		// public static이면 외부 호출 가능(접근제한을 사용할 수 있음)
	
	
	// 기능(method)에 대한 내용.... - 3) 동작
		//객체의 동작에 해당하는 실행 블록
	
}
